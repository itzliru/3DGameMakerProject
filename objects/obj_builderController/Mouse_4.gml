/// @description Insert description here
// You can write your code in this editor
cube_add(
    floor(mouse_x / global.grid_size) * global.grid_size,
    floor(mouse_y / global.grid_size) * global.grid_size,
    0
);
